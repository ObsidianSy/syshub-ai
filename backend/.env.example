# Database Configuration (Docker PostgreSQL)
# Para desenvolvimento local, use localhost
DB_HOST=localhost
DB_PORT=5432
DB_NAME=syshub
DB_USER=postgres
DB_PASSWORD=syshub2024

# Para produção/VPS com EasyPanel, use:
# DB_HOST=docker_iagente
# DB_NAME=docker
# DB_PASSWORD=sua_senha_do_easypanel

# Server Configuration
PORT=3001
NODE_ENV=development

# JWT Configuration
JWT_SECRET=syshub_super_secret_key_change_in_production_2024
JWT_EXPIRES_IN=7d

# CORS
CORS_ORIGIN=http://localhost:5173

# N8N Webhook para processamento de queries com agente de IA
# Configure a URL do webhook do N8N que irá processar as queries
# O webhook deve receber: { query_id, user_question, available_systems, conversation_id, metadata }
# E retornar: { answer, system_used, confidence, system_id }
N8N_WEBHOOK_URL=https://seu-n8n-instance.com/webhook/central-system-agente
